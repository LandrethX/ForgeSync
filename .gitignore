# macOS
.DS_Store

# Build output
bin/

# Release tarballs (make release); they belong on a GitHub release, not here
dist/

# Built web UI (make web); only the placeholder is committed so go:embed works
internal/webui/dist/*
!internal/webui/dist/.gitkeep
